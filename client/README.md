# Quantiom client

Vite + React + TypeScript. Install-free web UI.

```
npm install
npm run dev      # http://localhost:5173 (proxies /api → http://localhost:8000)
npm run build
npm run typecheck
```
